<h1>List of all Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
    </li>
  <% end %>
</ul><br>
<h4><%= link_to "List all Articles", articles_path %></h4>